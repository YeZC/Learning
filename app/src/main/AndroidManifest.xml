<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learningmaterials">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".java.annotation_processing.AnnotationActivity"></activity>
        <activity android:name=".architecture.mvp.MvpActivity" />
        <activity android:name=".architecture.mvc.MvcActivity" />
        <activity android:name=".architecture.mvvm.MvvmActivity" />
        <activity android:name=".custom_view.activity.DrawActivity" />
        <activity android:name=".custom_view.activity.LayoutActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>